# Ticketing Demo API (Placeholder)

This directory is reserved for the Fastify + Prisma API that powers the ticketing demo UI.

Suggested layout:

```
backend/
├── package.json
├── prisma/
│   └── schema.prisma
├── src/
│   ├── index.ts
│   └── routes/
│       ├── auth.ts
│       └── tickets.ts
└── README.md
```

Populate this area with your server implementation and update the root README with setup instructions when the backend is ready.
